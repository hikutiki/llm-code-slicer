# slice_funcs rebuild notes

## 構成

- `Index` が対象 Python ファイルを一度だけ `ast.parse` し、関数・メソッド・入れ子関数・クラスに完全名と行範囲を付けます。モジュールの import、代入、別名も `indexed_bindings` に記録し、最終代入を使って alias を解決します。
- 線は `EdgeProvider` の部品として独立させました。既定登録は `call / ctor / typed_attr / ref / alias / dispatch / nested / decorator` です。各部品は `name` と `edges(index)` だけを実装します。
- 新しい種類は `EdgeProvider` を継承した小さなクラスを作り、`EDGE_PROVIDERS` にインスタンスを1つ追加すれば `--kinds`、探索、`--edges` で利用できます。
- 探索は down と up を別々の visited 集合で BFS し、最後に結果だけを合わせます。層は各方向での最短距離です。`--layers` は深さ制限ではなく到達可能範囲の飽和までを表示します。
- `--compare-trace` は trace_calls.py の2列出力を読み、`<module>`、lambda、各種内包表記を除外して完全名へ正規化し、実トレース組数、静的に拾えた組数・割合、静的だけの組数、未捕捉一覧を出します。

## dispatch の判断

- `set_defaults(func=X)` を記述した関数から `X` へ線を張ります。
- 同じファイル内に `set_defaults(func=X)` がある場合、`args.func(...)` のような `.func(...)` を実行する関数からも候補 `X` へ線を張ります。実行時トレースの `main -> cmd_x` と比較できるようにするためです。複数サブコマンドがある場合は安全側に全候補へ張るため、静的だけの線は増え得ます。
- モジュール辞書 `{"name": X}` は、その辞書名を読む関数から `X` へ線を張ります。関数内辞書は辞書を作る関数から `X` へ張ります。

## 互換性と既存テストの変更

- CLI の既存引数は維持しました。`--attr-calls` は `typed_attr` を有効にする互換指定として残しています。
- 新仕様で「既定は全 kinds」になったため、旧テストの「typed attribute call は `--attr-calls` のときだけ有効」という期待だけを変更しました。明示的に `--kinds call` とすれば typed_attr を除外できます。
- `--edges` は仕様どおり `caller<TAB>callee<TAB>kind` の3列へ変更したため、その旧期待値を更新しました。
- 通常の範囲出力については、diff 起点で使われる import、モジュール定数、クラスのヘッダ/クラス代入、使用された `self.x` の代入を補う既存挙動を維持しています。名前指定だけの旧挙動も、不要なクラス補助行を増やさないよう維持しました。

## 自己検査

添付の `trace_calls.py` を使い、`argparse` の `set_defaults(func=cmd_a)` と `args.func(args)` を含む一時 CLI を実行して比較しました。実トレース（`<module>` を除く）は `main -> build` と `main -> cmd_a` の2組で、`--kinds call,dispatch` の静的線は2組とも捕捉し、比較結果は 2/2 = 100.00% でした。静的側には表を作る `build -> cmd_a` が1組余分に出ます。

## relevance 選択（今回追加）

`--select relevance --budget <行数|割合%>` は、差分または `--names` で得た出発点を必須にし、同じ静的グラフ上で到達できる記号を関連度で順位付けして予算内へ追加します。既定の `--select legacy` は従来経路のままで、`--depth` / `--callers` / `--max-lines` の既存挙動は変更していません。relevance では層数で候補を切らず、`--direction` の範囲で到達可能な候補を飽和まで見て点数で絞ります。

### 点数部品の足し方

点数は `ScoreProvider` の独立部品です。新しい物差しは次の3点だけで追加できます。

1. `ScoreProvider` を継承し、`name`、`default_weight`、`score(index, seeds, candidate, context=None)` を実装する。返り値は 0〜1。
2. `SCORE_PROVIDERS` にそのインスタンスを1つ追加する。
3. 高価な前計算が必要なら `_score_context()` に共有データを1項目追加する。単純な物差しならこの変更も不要。

`score_symbols(..., providers=[...])` と `_parse_weights(..., providers=[...])` も登録リストを引数で差し替えられるため、テストでは本体分岐を増やさず独自部品1個だけで拡張できることを確認しています。

既定部品は以下です。

- `distance` (1.0): 出発点から上り・下りを別 BFS で測り、近い側の `1/(1+距離)` を採用。最短距離1を明確に優先するため基準重みを1.0にしました。
- `edge_kind` (1.0): 経路で使った線を `call > ctor/typed_attr > dispatch/alias > ref` の順で評価。直接的な実行関係を距離と同程度に重視します。`nested/decorator` は dispatch/alias と同じ中間値です。
- `shared_state` (1.0): 出発点と同じ `self.x`、モジュール変数・定数を読む/書く記号を評価。制御フロー線に現れにくい結合なので距離と同じ重みにしました。
- `diff_terms` (0.8): 差分の追加・削除行にある識別子と文字列が候補に出る割合。名前の偶然一致によるノイズがあり得るため上記3種より少し低くしています。
- `cochange` (0.6): `--git-root` がある場合だけ直近 `--git-commits`（既定200）を調べ、出発点と同じコミットで変更された回数を最大値で正規化。履歴は強い補助信号ですが、古い構造や行移動の影響を受けるため既定では最も低くしています。git がない・失敗した場合は0として黙って継続します。

重みは `--weights distance=...,edge_kind=...` で部分上書きできます。0にするとその物差しを合成点から実質無効化できます。

### 行予算と大きな記号

`--budget 400` は400行、`--budget 20%` は対象ファイル総行数の20%を上限とします。出発点の関数は従来同様に必ず全体を含めるため、出発点だけで予算を超える場合は予算超過を許します。それ以外の候補は、候補本体と新たに必要になる補助定義を合わせた実増分が予算内に収まる場合だけ採用します。

relevance 専用の補助定義は、選んだ記号が実際に使う import、モジュール代入/定数、参照クラスのヘッダ、所有クラスのヘッダ、使用している `self.x` の代入だけです。未使用の名前定義は足しません。

出発点以外の関数が `--large-symbol-lines`（既定150行）を超える場合、関数全体の代わりに def 行と docstring、および出発点方向へ戻る静的辺の行の前後 `--context-lines`（既定15行）を採用します。内部に「出発点へ戻る辺」がない下り側の巨大関数では、def/docstring だけになる場合があります。これは、根拠のない中央部分を予算へ入れるより、必要なら次回その関数を新しい出発点として再スライスできる設計を優先した判断です。

`--explain` は採用した記号ごとに合成点、各部品の0〜1点、巨大関数の切り詰め有無を標準エラーへ1行で出します。標準出力の `path:start-end,...` 形式は変えません。

### 判断に迷った点

- relevance で既存 `--depth` / `--callers` を候補上限に使うと、層による量の振れを避ける目的と衝突するため、relevance の候補集合は `--direction` 内で飽和まで探索し、距離は点数としてだけ使うことにしました。
- 巨大な出発点自身を切り詰めると「変更関数を必ず含める」の意味が弱くなるため、出発点だけは全体を保持します。巨大候補だけを切り詰めます。
- `cochange` は標準ライブラリ制約のため Python 用 git ライブラリを使わず、`git log` と各コミットの `git show --unified=0` を subprocess で読みます。現在ファイルの関数範囲へ変更後行を突き合わせる方式なので、大規模な行移動を含む古い履歴では近似です。この信号を既定0.6に抑えた理由でもあります。
