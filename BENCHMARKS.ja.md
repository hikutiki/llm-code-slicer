# 検証データ

[English](BENCHMARKS.md)

数字はすべて、1つの非公開のコード（Python のファイル3つ）で 2026-09-25 に測ったものです。確認の実行は **各1回** なので、数千トークンの差や指摘1つの違いは誤差とみなしてください。生の記録は非公開の関数名を含むので出していません。測ったものは下の表ですべてです。

使ったファイル:

| ここでの呼び名 | 行数 | 形 |
|---|---|---|
| CLI | 5,811 | argparse のサブコマンドを `set_defaults(func=...)` で振り分ける |
| 反映スクリプト | 3,351 | 大きなクラス1つ、1,250 行の自己検査の関数 |
| ジョブの本体 | 10,226 | 関数 230 個、`main` が1つ |

## 1. 静的な地図は本物の呼び出しと合っているか

各ファイルの自己検査を `trace_calls.py`（`sys.settrace`）の下で実行し、本物の呼び出しを記録。同じファイルの関数から同じファイルの関数への呼び出しだけを数え、lambda・内包表記・モジュールの最上位は除外。

| ファイル | 道具の版 | 本物の組 | 一致 | 静的だけの組 |
|---|---|---|---|---|
| 反映スクリプト | 呼び出し＋型の分かる受け手だけ | 89 | 62（69%） | 3 |
| 反映スクリプト | 今の版（全ての線の種類） | 76 | 71（93%） | 8 |
| CLI | 今の版（全ての線の種類） | 183 | 182（99%） | 56 |

前の版は、クラスの生成（`Class(...)` → `__init__`）、関数の中の関数、変数に入れた関数を見落としていた。「静的だけの組」の多くは、自己検査が通らなかった道。

## 2. `main` から下りだけでどこまで届くか

`--names main --direction down --layers --metric code`（空行・コメントを除いたコードの行）。層ごとに増えた割合:

| ファイル | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 合計 | 頭打ち |
|---|---|---|---|---|---|---|---|---|---|
| 反映スクリプト | 2% | 42% | 34% | 17% | 1% | | | 95% | 4層 |
| ジョブの本体 | 1% | 4% | 27% | 38% | 14% | 4% | 1% | 90% | 6層 |
| CLI（`dispatch` の線の前） | 0% | 2% | | | | | | 2% | 1層 |
| CLI（今の版） | 0% | 2% | 50% | 34% | 8% | 0.3% | | 95% | 5層 |

上から見るとプログラムはピラミッド型で、入口から 4〜6 層で 9割以上に届く。層ごとの量は真ん中が厚い（ひし形に近い）。表で処理を選ぶ CLI は、表そのものを線にするまで届かないように見えた。

向きを混ぜる（呼び元の呼び元、その呼び先…）と、数層でほぼ全部が関係あるように見えてしまう。呼び元と呼び先を別々にたどるのはそのため。

## 3. 確認する側はどれだけのコードが要るか

CLI のファイルへの1つの変更（差分 36 行）。依頼文は毎回同じで、添えるコードだけを変えた。確認役: 中くらいのコーディング用モデル（考える量 medium）、読むだけ、道具なし。

| 添えたもの | 行数 | 入力 | 出力 | 判定 | 主な指摘 |
|---|---|---|---|---|---|
| 関連の強さで選択（予算1割） | 418 | 25,058 | 675 | PASS | 中心の問題＋全文では出なかった自己検査の穴。渡していない所は「不明」と書いた |
| 行数の上限1割 | 606 | 27,345 | 512 | PASS | 中心の問題 |
| 行数の上限2割 | 1,194 | 36,957 | 486 | PASS | 中心の問題 |
| 行数の上限3割 | 1,788 | 47,872 | 522 | FAIL | 同じ問題を HIGH と判定 |
| 行数の上限4割 | 2,376 | 61,018 | 743 | FAIL | 同じ問題を HIGH と判定、自己検査の穴 |
| 行数の上限5割 | 2,933 | 70,713 | 561 | PASS | 中心の問題 |
| 呼び元1層 | 1,550 | 45,356 | 445 | PASS | 中心の問題 |
| 呼び元2層 | 4,649 | 108,495 | 632 | PASS | 中心の問題 |
| 全文 | 5,811 | 105,340 | 594 | PASS | 中心の問題 |

- どの回も同じ中心の問題を見つけた。PASS／FAIL が分かれたのは、同じ指摘を MED と付けるか HIGH と付けるかが回ごとにぶれたためで、コードの量とは関係が見えなかった。
- 層単位で切ると、次の層に大きな呼び元があると一気にふくらむ（ここでは2層でほぼ全文）。関連の強さで選ぶと、大きな関数は変更につながる行だけに切り詰める。

2つ目の変更（反映スクリプト、medium で確認）も同じ傾向: 全文 入力 6.3万／PASS、呼び元つきの切り出し 4.9万／PASS。呼び元を**入れない**切り出しでは変更の影響を判定できず「不明」になった — 呼び元は入れること。

## 4. 足りない関数を確認役に名指しさせる

わざと足りない切り出しから始め、足りない関数を `NEED: 名前` で挙げさせて、足して2回目を回した。

| 条件 | 1回目 | 2回目 | 入力の合計 |
|---|---|---|---|
| 目次なし | 3つ名指し、うち1つは実在しない | FAIL、別の実在しない関数を要求 | 約9.1万 |
| 目次つき（`--toc`） | 実在する1つを名指し | FAIL（渡したコードを読み違えた） | 約9.1万 |

2回回すと、全文1回（6.3万）より高い。確認役には、呼び元つきの関連の強さの切り出しを1回で渡す。`NEED` は補助にとどめ、使うときは必ず目次を付ける。

## 5. 小さなローカルのモデルは、切り出しで不具合を直せるか

反映スクリプト（3,416 行）の1つの関数に不具合を入れた（パスの重複の確認が大文字小文字を区別するようになった）。モデルには不具合の報告・それを入れた差分・コードを1回だけ渡し（テストを回す往復なし）、直した関数を返させた。モデル: Ollama の `qwen3.5:9b`（temperature 0.2）。返事を元の位置に差し替えて確かめた。

| 渡したもの | 行数 | 入力 | 出力 | 時間 | 直ったか |
|---|---|---|---|---|---|
| 関連の強さの切り出し（予算1%） | 36 | 856 | 1,044 | 90 秒 | 直った |
| 同（予算5%） | 158 | 2,833 | 559 | 48 秒 | 直った |
| 同（予算10%） | 277 | 4,597 | 796 | 72 秒 | 直った |
| 全文 | 3,416 | 56,256 | 905 | 546 秒 | 直った |

どれも1回で直した。全文は入力が約66倍、時間が7〜11倍。不具合は差分から場所が分かるやさしいものだったので、差は正しさより速さと入力の量に出た。

## 自分のコードで測り直す

```sh
# 1. テストを実行しながら本物の呼び出しを記録する
python3 trace_calls.py --file app.py -- <テストを動かすのに必要な引数> > real.tsv
# 2. 静的な地図と比べる
python3 slice_funcs.py --file app.py --compare-trace real.tsv
# 3. main からどこまで届くか
python3 slice_funcs.py --file app.py --names main --direction down --layers --metric code
# 4. 確認用に大きさの違う切り出しを作る
python3 slice_funcs.py --file app.py --diff change.diff --select relevance --budget 10% --git-root . --explain
python3 slice_funcs.py --file app.py --diff change.diff --max-lines 600
```
